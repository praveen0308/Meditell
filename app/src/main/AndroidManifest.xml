<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jmm.brsap.meditell">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meditell">
        <activity
            android:name=".ui.schedule.CreateSchedule"
            android:exported="true" />
        <activity
            android:name=".ui.schedule.ManageSchedule"
            android:exported="true" />
        <activity
            android:name=".ui.interaction.AddNewInteraction"
            android:exported="true" />
        <activity
            android:name=".ui.AddSchedule"
            android:exported="true" />
        <activity
            android:name=".ui.welcome.MainDashboard"
            android:exported="true" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>